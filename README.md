# MUI Theme Customizer

A visual theme builder for Material UI, modeled after inspireUI's
theme generator. See `CLAUDE.md` and `_context/` for the full
product and architecture spec.

## Development

```
npm install
npm run dev
```

## Build

```
npm run build
```
